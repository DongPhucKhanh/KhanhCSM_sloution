### 🚀 Buổi 2: Xây dựng RESTful API và Admin Panel cơ bản
* **Nhiệm vụ:** Viết các Endpoint giao tiếp và giao diện quản trị viên.
* **Chi tiết công việc:**
  * Viết các `Controllers` chứa phương thức GET, POST, PUT, DELETE cho Danh mục, Phụ tùng và Tin tức.
  * Xây dựng các trang Razor Views quản trị (CRUD) cho `Category`, `Product`, `User`.
  * Tích hợp cấu hình CORS (`AllowAnyOrigin`) vào `Program.cs` để mở cổng cho ReactJS sau này.
  * Tích hợp thư viện Swashbuckle để tự động sinh tài liệu API (Swagger UI).
  * 
